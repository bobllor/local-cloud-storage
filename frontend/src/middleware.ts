/**
 * A request will be sent to the backend for authentication using the cookie
 * in the request header.
 * 
 * Authentication failures will redirect to the login page.
 */
export async function authMiddleware(){

}